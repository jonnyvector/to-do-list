import { addProject } from "./addProject";

//check to see which project is selected
// add project to project's array

console.log(addProject().projectList);

function addTaskToProject(task) {}

export { addTaskToProject };
